apiVersion: v1
kind: Service
metadata:
  name: node-cache-api
  labels:
    name: node-cache-api
spec:
  type: "{{.Values.service.type}}"
  ports:
  - name: http
    port: {{.Values.service.port}}
    targetPort: {{.Values.service.port}}
  selector:
    app: node-cache-api